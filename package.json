{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Full stack application",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm install && npm run build && cd .. && mkdir -p backend/dist/frontend && cp -r frontend/dist/* backend/dist/frontend/",
    "build:backend": "cd backend && npm install && npx tsc",
    "start": "node backend/dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
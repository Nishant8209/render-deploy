{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Full stack application",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm install && npm run build && cd .. && mkdir -p dist/frontend && cp -r frontend/dist/* dist/frontend/",
    "build:backend": "cd backend && npm install && npx tsc && cd .. && mkdir -p dist/backend && cp -r backend/dist/* dist/backend/",
    "start": "node dist/backend/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}